---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "libredesk.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "libredesk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "libredesk.selectorLabels" . | nindent 4 }}

